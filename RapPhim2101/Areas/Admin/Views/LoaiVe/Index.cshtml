@model IEnumerable<RapPhim2101.Models.LoaiVe>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Tạo mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Tên Loại Vé
        </th>
        <th>
            Đơn Giá
        </th>

        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenLv)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DonGia)
            </td>
            
            <td>
                @Html.ActionLink("Chỉnh sửa", "Edit", new { id = item.MaLV }) |
                @Html.ActionLink("Chi tiết", "Details", new { id = item.MaLV }) |
                @Html.ActionLink("Xóa", "Delete", new { id = item.MaLV })
            </td>
        </tr>
    }

</table>
